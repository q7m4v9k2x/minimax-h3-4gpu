# 15 秒分段端到端验收（2026-09-24）

本记录对应提交 `4dfeba6`，只记录公开的参数和媒体校验，不包含模型权重、凭据或完整用户提示词。

## 请求与路径

- 公网任务：`22f6c4c0f1847484a1eb08ee`
- 画幅：864×480，24 FPS
- 每段：124 帧、20 次模型评估；共 3 段，seed 从 `20260924` 递增
- 请求：`duration=15, segments=3, lossless=false`
- 路径：真实 conditioning → 三次四卡 TP4 DiT → 三次官方视频/音频 VAE → FFmpeg 拼接
- 服务：工作站 `minimax-h3.service`，`/home/ymzx/ComfyUI/venv/bin/python` + 同环境 `torchrun`

## 结果

- 阶段事件完整经过 `conditioning → loading → sampling → decode`（三段）→ `encode`。
- 三段采样都完成 20/20；任务最终 `completed`，总体进度 `16/16`。
- 原始拼接输出曾是 H.264 4:4:4；部分 Windows 播放器不接受该 profile。桌面交付已转为标准 H.264 High、`yuv420p` + AAC，864×480，360 帧，24/1 FPS，视频/音频/容器时长均为 15.000000 秒。后端默认编码已同步改为该兼容格式；RGB 无损版本仍作为可选归档输出。
- 文件大小：58,877,109 bytes。
- 下载成品：`C:\Users\Administrator\Desktop\minimax-h3-15s-22f6c4c0.mp4`
- 本机 `ffprobe -count_frames` 和 FFmpeg `-xerror` 解码检查通过；中间帧人工抽检画面可解码。
- 任务完成后四张 V100 回到 0% 利用率、约 5 MiB 显存，服务保持 active，公网 `/h3-api/health` 返回 `ready=true`。

## 边界

这是三个独立 124 帧片段的受控拼接，不是单次 362 帧长序列。因此片段边界可能有跳切或运动连续性变化；它解决的是 4×16GB 容量约束下的可用 15 秒交付。需要连续长镜头时仍应研究时间分块或更大显存，不能把本记录当成单次长序列证明。
